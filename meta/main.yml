galaxy_info:
  author: DANS-KNAW
  description: Ansible role that creates an EASY LDAP directory in openldap and populates it with basic entries.
  company: "DANS-KNAW"
  license: "Apache 2.0"
  min_ansible_version: 2.3
  platforms:
    - name: EL
  galaxy_tags:
    # Add tags to classify this module

allow_duplicates: yes
